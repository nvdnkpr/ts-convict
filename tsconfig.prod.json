{
  // This is used by `ts language service` and testing.
  // Includes source and test files.
  "extends": "./tsconfig.json",
  "compilerOptions": {
      "sourceMap": false,
      "pretty": false,
      "removeComments": true
  },
  "exclude": [
    "src/test"
  ]
}
